|  # | Issue title                                                                                                           | Category       | Severity     | Exact location / reference                                        | Detailed evidence                                                                                                                                                                                                                                                                                  | Why this is a bug or risk                                                                                                                                                                                                                                                                                | Potential impact                                                                                                                                                                                  | Suggested fix / mitigation                                                                                                                                                                                                                   |
| -: | --------------------------------------------------------------------------------------------------------------------- | -------------- | ------------ | ----------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
|  1 | Project profitability report includes non-completed projects and calculates revenue/profit before official completion | Business logic | **Critical** | `src/actions/project-profitability-actions.ts`                    | When `filters.status !== "completed"`, the report includes `completed`, `installation_completed`, and `in_progress` projects.  It then computes `totalRevenue`, `totalCogs`, `totalExpenses`, `grossProfit`, `netProfit`, and margins from journal data for those projects.                        | The task definition explicitly treats income, revenue, profit, commission, payout, or financial calculation before official completion as a business logic error. This report calculates revenue and profit for `in_progress` and `installation_completed` projects when the default/all filter is used. | Financial reports can show revenue and profit from projects that are not officially completed, causing premature recognition, misleading profitability KPIs, and incorrect management decisions.  | Restrict profitability calculations to `projects.status = "completed"` by default. If non-completed projects must appear, label them as pipeline/WIP and suppress revenue/profit metrics or show clearly separated non-accounting estimates. |
|  2 | Invoice posting recognizes revenue without checking that the project is completed                                     | Business logic | **Critical** | `src/actions/invoice-actions.ts`                                  | `postInvoice` fetches the invoice by ID and only checks whether the invoice status can be posted.  It then posts a journal entry debiting accounts receivable and crediting `solar_installation_revenue`.  There is no project lookup or `projects.status === "completed"` check in this function. | Posting an invoice creates recognized income/revenue before verifying official project completion. This directly matches the business-logic error definition.                                                                                                                                            | Revenue and AR can be recognized for draft, planning, in-progress, on-hold, or installation-completed projects. This can corrupt P&L, dashboard revenue, receivables, and tax/accounting reports. | In `postInvoice`, join/fetch the related project and require `status === "completed"` before creating the revenue journal entry. Consider allowing draft invoice creation earlier, but only posting after completion.                        |
|  3 | Invoice creation allows arbitrary `customerId` instead of deriving it from the project                                | Data handling  | **High**     | `src/actions/invoice-actions.ts`, `src/lib/validators/invoice.ts` | The schema accepts both `projectId` and `customerId` from caller input.  `createInvoice` inserts both values directly into `projectInvoices`.  There is no evidence in this function that the customer belongs to the project.                                                                     | The server trusts client-provided relational data. A caller can create an invoice where `projectId` points to one customer’s project and `customerId` points to a different customer.                                                                                                                    | Invoices can be associated with the wrong customer, producing incorrect receivables, PDFs, customer account histories, and audit records.                                                         | Remove `customerId` from the public create-invoice input. Fetch the project server-side, derive `customerId` from the project row, and reject if the project does not exist.                                                                 |
|  4 | Invoice creation does not validate invoice date / due date ordering                                                   | Data handling  | **Medium**   | `src/lib/validators/invoice.ts`, `src/actions/invoice-actions.ts` | The schema validates both dates as datetimes but has no refinement enforcing `dueDate >= invoiceDate`.  `createInvoice` stores both dates directly.                                                                                                                                                | An invoice can be created with a due date before the invoice date. This is not a syntax error, but it is a hidden data-quality bug that can distort aging and collection workflows.                                                                                                                      | Receivable-aging reports and overdue reminders can be wrong immediately upon invoice creation. Users may see invoices as overdue before they were issued.                                         | Add a schema refinement requiring `dueDate` to be on or after `invoiceDate`. Also consider normalizing dates to a consistent timezone/date-only representation if business rules are date-based.                                             |
|  5 | Payment recording allows payments on cancelled or non-completed projects                                              | Business logic | **High**     | `src/actions/payment-actions.ts`                                  | `recordPayment` fetches the project only to verify existence.  It does not reject cancelled projects or require completion before recording a final payment. It then writes `projectPayments` and posts either customer deposits or accounts receivable journal lines.                             | For final payments especially, the function performs financial posting without enforcing project lifecycle rules. The UI may disable payment on cancelled projects, but the server action itself does not enforce that invariant.                                                                        | Direct server-action calls or future UI paths can record payments against cancelled or inappropriate project states. This can corrupt cash, deposits, AR, and project payment history.            | Enforce allowed project statuses in `recordPayment`. For `paymentType === "final"`, require the project to be `completed` and have sufficient AR. For advances, define explicit allowed statuses and reject `cancelled`.                     |
|  6 | Payment allocation silently ignores missing invoice IDs                                                               | Hidden bug     | **High**     | `src/actions/payment-actions.ts`                                  | During allocation, the code fetches each invoice by ID and does `if (!invoice) continue;`.  The payment and journal entry have already been created in the same transaction.                                                                                                                       | Invalid invoice allocations are silently dropped. The caller can receive success for a payment whose requested allocation was not applied.                                                                                                                                                               | Payments can be recorded as cash received while invoice balances remain open. This creates reconciliation mismatches and confusing user-facing state.                                             | Replace `continue` with an error such as `invoice_not_found`, and roll back the transaction. Validate all allocation invoice IDs before inserting the payment.                                                                               |
|  7 | Payment allocation does not verify that allocated invoices belong to the payment’s project                            | Data handling  | **High**     | `src/actions/payment-actions.ts`                                  | The invoice query filters only by `projectInvoices.id`.  The inserted allocation links the created payment to that invoice ID.  There is no condition requiring `invoice.projectId === data.projectId`.                                                                                            | A payment for one project can be allocated to an invoice from another project. This is a cross-project accounting integrity bug.                                                                                                                                                                         | Project A can show cash received while Project B’s invoice is reduced or marked paid. AR, profitability, customer balances, and audit trails become unreliable.                                   | Query invoices with both `id` and `projectId`, or explicitly compare `invoice.projectId` to `data.projectId` and throw on mismatch.                                                                                                          |
|  8 | Payment allocation can overpay an invoice while forcing balance due to zero                                           | Logic          | **High**     | `src/actions/payment-actions.ts`                                  | New paid amount is calculated as current paid plus allocation amount. Balance due is clamped with `Math.max(0, Number(invoice.total) - newPaidAmount)`, and status becomes `paid` when the clamped balance is zero.  There is no check that `alloc.amount <= invoice.balanceDue`.                  | Clamping hides overpayment instead of rejecting or separately accounting for it. The invoice’s `paidAmount` can exceed `total` while `balanceDue` is zero.                                                                                                                                               | Overpayments can disappear from invoice balance logic, paid totals can exceed invoice totals, and customer credit/deposit accounting can be wrong.                                                | Reject allocations greater than the current `balanceDue`, or explicitly post/track customer credits for overpayments.                                                                                                                        |
|  9 | Project detail profitability calculates profit for any project detail page, regardless of completion status           | Business logic | **High**     | `src/actions/project-actions.ts`                                  | `getProject` computes `invoicedRevenue`, `grossProfit`, `netProfit`, and margin percentages directly from non-draft/non-void invoices and active costs.  The cited calculation is not guarded by `row.status === "completed"` in the shown code.                                                   | This is a project-level financial calculation and can occur before official completion. It may be intended as operational visibility, but per the task definition this is a business-logic issue unless explicitly separated from recognized profit.                                                     | Users can see apparent profitability before the project is officially complete, encouraging premature accounting conclusions or operational decisions.                                            | Either compute these fields only for completed projects, or rename/separate them as “estimated/WIP profitability” and exclude them from accounting reports.                                                                                  |
| 10 | Dashboard AR estimate uses quoted project total minus payments instead of invoice/ledger AR                           | Logic          | **Medium**   | `src/actions/dashboard-actions.ts`                                | Dashboard AR count/amount calculates `projects.quotedTotal - sum(projectPayments.amount)` per project.  This ignores posted invoices, invoice balances, voided invoices, customer deposits, and accounts-receivable journal balances.                                                              | Accounts receivable should come from posted invoice balances or ledger AR, not quoted total. Advance payments and unposted invoices can make this estimate materially different from true AR.                                                                                                            | Dashboard AR can overstate receivables for unbilled work, understate/overstate projects with partial invoices, and conflict with finance reports.                                                 | Replace this query with posted invoice `balanceDue` totals or ledger `accounts_receivable` balances, filtered to unreversed entries and appropriate invoice statuses.                                                                        |
| 11 | Dashboard “total revenue” uses quoted total for completed projects, not posted revenue                                | Logic          | **Medium**   | `src/actions/dashboard-actions.ts`                                | `totalRevenue`, `thisMonthRevenue`, and `prevMonthRevenue` sum `projects.quotedTotal` for completed projects.                                                                                                                                                                                      | Quoted contract value is not necessarily recognized revenue. Discounts, amendments, partial/void invoices, tax treatment, and posted revenue journals may differ from the quote.                                                                                                                         | Revenue KPIs can disagree with finance reports and ledger-based P&L. Management may see inflated or stale revenue.                                                                                | Use ledger income or posted invoice totals as the source of revenue. If the intended metric is contract value, rename it to “completed contract value.”                                                                                      |
| 12 | Project status workflow allows reopening cancelled projects directly to planning                                      | Logic / UX     | **Medium**   | `src/lib/domain/project.ts`                                       | Status transitions define `cancelled: ["planning"]`.  `updateProject` permits transitions if `canTransitionProjectStatus` returns true.                                                                                                                                                            | Reopening a cancelled project can be valid in some businesses, but doing it directly without reversal/reinstatement checks is risky because cancellation may already have financial, inventory, schedule, or notification side effects.                                                                  | Cancelled projects can become active again while prior cancellation artifacts remain. This can create audit ambiguity and inconsistent operational state.                                         | Require an explicit “reopen” action with audit logging and checks for reversed costs, invoices, payments, warranty alerts, and notifications. Alternatively remove the transition if cancellation is final.                                  |
